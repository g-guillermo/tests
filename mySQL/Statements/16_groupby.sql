SELECT MAX(age) FROM users GROUP BY age;

-- Agrupa resultados por una columna y obtiene el valor maximo de cada grupo

SELECT COUNT(age) FROM users GROUP BY age;

-- Agrupa resultados por una columna y cuenta el numero de resultados en cada grupo